import React, { FormEvent, FormEventHandler, useState } from 'react';
import { Form, FormControl } from 'react-bootstrap';


// idea, if encounter a \n character then add a div then add the spans

const TimerDisplay = () => {
    
}

export default TimerDisplay ;